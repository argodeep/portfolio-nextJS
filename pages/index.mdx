---
title: Arghyadeep Majumder | Senior Engineer & Solution Architect
---

<style>{`
@import url('https://fonts.googleapis.com/css2?family=Gabarito:wght@400;500;600;700;800;900&family=Be+Vietnam+Pro:wght@300;400;500;600&display=swap');

/* ── TOKENS ── */
:root {
  --color-bg:       #0e141b;
  --color-surface:  #131920;
  --color-surface-2:#161e26;
  --color-border:   rgba(226,232,240,0.10);
  --color-divider:  rgba(226,232,240,0.08);
  --color-text:     #e2e8f0;
  --color-muted:    #94a3b8;
  --color-faint:    #4a5568;
  --color-primary:  #2563eb;
  --font-serif:     'Gabarito', sans-serif;
  --font-sans:      'Be Vietnam Pro', sans-serif;
  --space-1:0.25rem;--space-2:0.5rem;--space-3:0.75rem;
  --space-4:1rem;--space-5:1.25rem;--space-6:1.5rem;
  --space-8:2rem;--space-10:2.5rem;--space-12:3rem;
  --space-16:4rem;--space-20:5rem;--space-24:6rem;
  --radius-sm:0.375rem;--radius-md:0.5rem;--radius-lg:0.75rem;--radius-full:9999px;
  --transition:180ms cubic-bezier(0.16,1,0.3,1);
  --max-w:48rem;
}

*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
html{scroll-behavior:smooth;-webkit-font-smoothing:antialiased;text-rendering:optimizeLegibility;scroll-padding-top:80px;}
body{font-family:var(--font-sans);color:var(--color-text);background:var(--color-bg);line-height:1.65;min-height:100dvh;}
img{display:block;max-width:100%;height:auto;}
p,li{text-wrap:pretty;max-width:72ch;}
h1,h2,h3,h4{text-wrap:balance;line-height:1.1;}
a{color:inherit;text-decoration:none;transition:color var(--transition);}
button{cursor:pointer;background:none;border:none;font:inherit;color:inherit;}
::selection{background:rgba(37,99,235,0.25);color:var(--color-text);}
:focus-visible{outline:2px solid var(--color-primary);outline-offset:3px;border-radius:var(--radius-sm);}

/* ── NAV ── */
.nav{
  position:fixed;top:0;left:0;right:0;z-index:100;
  background:rgba(14,20,27,0.88);
  backdrop-filter:blur(12px);
  -webkit-backdrop-filter:blur(12px);
  border-bottom:1px solid var(--color-border);
}
.nav-inner{
  max-width:var(--max-w);margin-inline:auto;
  padding:var(--space-4) var(--space-5);
  display:flex;align-items:center;justify-content:space-between;
}
.nav-logo{
  font-family:var(--font-serif);font-size:1rem;font-weight:700;
  letter-spacing:-0.5px;color:var(--color-text);
}
.nav-links{display:flex;gap:var(--space-6);list-style:none;}
.nav-links a{font-size:0.875rem;color:var(--color-muted);font-weight:400;transition:color var(--transition);}
.nav-links a:hover{color:var(--color-text);}
.nav-cta{
  font-size:0.875rem;font-weight:500;color:#fff;
  background:var(--color-primary);
  padding:var(--space-2) var(--space-5);
  border-radius:var(--radius-full);
  transition:opacity var(--transition);
}
.nav-cta:hover{opacity:0.88;color:#fff;}
.nav-hamburger{display:none;flex-direction:column;gap:5px;padding:var(--space-2);}
.nav-hamburger span{display:block;width:20px;height:1.5px;background:var(--color-muted);}
.nav-mobile-menu{
  display:none;position:fixed;inset:0;top:61px;
  background:var(--color-bg);z-index:99;
  padding:var(--space-8) var(--space-5);
  flex-direction:column;gap:var(--space-6);
  border-top:1px solid var(--color-border);
}
.nav-mobile-menu.open{display:flex;}
.nav-mobile-menu a{font-size:1.5rem;font-family:var(--font-serif);font-weight:700;color:var(--color-text);border-bottom:1px solid var(--color-divider);padding-bottom:var(--space-5);}

/* ── PAGE WRAPPER ── */
.page-wrap{max-width:var(--max-w);margin-inline:auto;padding-inline:var(--space-5);}

/* ── HERO ── */
.port-hero{padding-top:120px;padding-bottom:var(--space-24);}
.hero-name{
  font-family:var(--font-serif);
  font-size:clamp(3.75rem,10vw,6rem);
  font-weight:900;
  letter-spacing:-2px;
  color:var(--color-text);
  margin-bottom:var(--space-2);
  line-height:1;
}
.hero-specialty{
  font-family:var(--font-serif);
  font-size:clamp(1.75rem,5vw,3rem);
  font-weight:800;
  letter-spacing:-1px;
  color:var(--color-primary);
  margin-bottom:var(--space-8);
  line-height:1.1;
}
.hero-summary{
  font-size:1.125rem;
  color:var(--color-muted);
  line-height:1.75;
  max-width:55ch;
  margin-bottom:var(--space-10);
}
.hero-summary strong{color:var(--color-text);font-weight:500;}
.hero-cta{
  display:inline-block;
  font-family:var(--font-sans);
  font-size:0.9375rem;
  font-weight:500;
  color:#fff;
  background:var(--color-primary);
  padding:var(--space-4) var(--space-8);
  border-radius:var(--radius-full);
  transition:opacity var(--transition);
}
.hero-cta:hover{opacity:0.85;color:#fff;}
.hero-links{display:flex;align-items:center;gap:var(--space-6);flex-wrap:wrap;}
.hero-link-plain{font-size:0.875rem;color:var(--color-muted);font-weight:400;transition:color var(--transition);}
.hero-link-plain:hover{color:var(--color-text);}

/* ── SECTION ── */
.port-section{padding-block:var(--space-24);}
.section-heading{
  font-family:var(--font-serif);
  font-size:clamp(2rem,5vw,2.75rem);
  font-weight:800;
  letter-spacing:-1px;
  color:var(--color-text);
  margin-bottom:var(--space-12);
}
.section-divider{border:none;border-top:1px solid var(--color-divider);margin-block:0;}

/* ── WORK / HOW I WORK ── */
.how-body p{font-size:1rem;color:var(--color-muted);line-height:1.8;margin-bottom:var(--space-5);}
.how-body p:last-child{margin-bottom:0;}
.how-body strong{color:var(--color-text);font-weight:500;}

/* ── TAGS ── */
.tags{display:flex;flex-wrap:wrap;gap:var(--space-2);margin-top:var(--space-8);}
.tag{
  font-size:0.8125rem;font-weight:500;letter-spacing:0.02em;
  color:var(--color-muted);
  border:1px solid var(--color-border);
  padding:var(--space-1) var(--space-3);
  border-radius:var(--radius-full);
}

/* ── IMPACT ── */
.impact-list{border-top:1px solid var(--color-divider);}
.impact-item{
  display:grid;grid-template-columns:2.5rem 1fr;gap:var(--space-6);
  padding:var(--space-8) 0;border-bottom:1px solid var(--color-divider);
  align-items:start;
}
.impact-num{font-family:var(--font-serif);font-size:1rem;font-weight:700;color:var(--color-faint);padding-top:2px;}
.impact-text{font-size:1rem;color:var(--color-muted);line-height:1.8;}
.impact-text strong{color:var(--color-text);font-weight:500;}

/* ── AI STORY ── */
.ai-section{
  background:var(--color-surface);
  border:1px solid var(--color-border);
  border-radius:var(--radius-lg);
  padding:var(--space-12);
}
.ai-label{font-size:0.75rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-faint);margin-bottom:var(--space-5);}
.ai-quote{
  font-family:var(--font-serif);
  font-size:clamp(1.25rem,3vw,1.75rem);
  font-weight:800;
  color:var(--color-text);
  line-height:1.25;
  margin-bottom:var(--space-8);
  letter-spacing:-0.5px;
  max-width:42ch;
}
.ai-body{font-size:1rem;color:var(--color-muted);line-height:1.85;margin-bottom:var(--space-10);max-width:60ch;}
.ai-body strong{color:var(--color-text);font-weight:500;}
.ai-body em{font-style:italic;color:rgba(226,232,240,0.75);}
.ai-metrics{
  display:grid;grid-template-columns:repeat(3,1fr);
  gap:1px;background:var(--color-border);
  border:1px solid var(--color-border);
  border-radius:var(--radius-md);overflow:hidden;margin-bottom:var(--space-8);
}
.ai-metric{background:var(--color-bg);padding:var(--space-6);}
.ai-metric-val{
  font-family:var(--font-serif);
  font-size:clamp(1.5rem,3vw,2rem);
  font-weight:900;letter-spacing:-1px;
  color:var(--color-text);line-height:1;margin-bottom:var(--space-2);
}
.ai-metric-label{font-size:0.75rem;color:var(--color-faint);letter-spacing:0.06em;text-transform:uppercase;font-weight:500;}
.ai-footnote{font-size:0.75rem;color:var(--color-faint);line-height:1.65;}

/* ── PROJECTS ── */
.projects-list{display:flex;flex-direction:column;border-top:1px solid var(--color-divider);}
.project-item{
  display:grid;grid-template-columns:1fr auto;
  gap:var(--space-6);align-items:start;
  padding:var(--space-8) 0;border-bottom:1px solid var(--color-divider);
  text-decoration:none;
}
a.project-item:hover .project-title{color:var(--color-primary);}
.project-title{font-family:var(--font-serif);font-size:1.125rem;font-weight:700;color:var(--color-text);margin-bottom:var(--space-2);transition:color var(--transition);}
.project-desc{font-size:0.9375rem;color:var(--color-muted);line-height:1.7;}
.project-arrow{font-size:1.25rem;color:var(--color-faint);align-self:center;flex-shrink:0;}

/* ── RECOGNITION ── */
.recognition-list{border-top:1px solid var(--color-divider);}
.recognition-item{
  display:flex;justify-content:space-between;align-items:start;
  gap:var(--space-6);padding:var(--space-7) 0;
  border-bottom:1px solid var(--color-divider);
}
.recognition-title{font-family:var(--font-serif);font-size:1rem;font-weight:700;color:var(--color-text);margin-bottom:var(--space-1);}
.recognition-org{font-size:0.875rem;color:var(--color-muted);}
.recognition-badge{
  font-size:0.75rem;font-weight:500;white-space:nowrap;
  color:var(--color-muted);
  border:1px solid var(--color-border);
  padding:var(--space-1) var(--space-3);
  border-radius:var(--radius-full);
  flex-shrink:0;
}

/* ── CONSULTING ── */
.consulting-body p{font-size:1rem;color:var(--color-muted);line-height:1.8;margin-bottom:var(--space-5);}
.consulting-body p strong{color:var(--color-text);font-weight:500;}
.cta-row{display:flex;gap:var(--space-4);flex-wrap:wrap;margin-top:var(--space-6);}
.btn-primary{
  display:inline-flex;align-items:center;gap:var(--space-2);
  font-size:0.9375rem;font-weight:500;color:#fff;
  background:var(--color-primary);
  padding:var(--space-4) var(--space-8);
  border-radius:var(--radius-full);
  transition:opacity var(--transition);
}
.btn-primary:hover{opacity:0.85;color:#fff;}
.btn-ghost{
  display:inline-flex;align-items:center;gap:var(--space-2);
  font-size:0.9375rem;font-weight:400;color:var(--color-muted);
  padding:var(--space-4) var(--space-8);
  border-radius:var(--radius-full);
  border:1px solid var(--color-border);
  transition:color var(--transition),border-color var(--transition);
}
.btn-ghost:hover{color:var(--color-text);border-color:rgba(226,232,240,0.22);}

/* ── PHOTO (polaroid) ── */
.photo-wrap{
  display:flex;justify-content:flex-start;
  margin-bottom:var(--space-10);
}
.polaroid{
  background:#fff;
  padding:var(--space-3) var(--space-3) var(--space-8);
  box-shadow:0 4px 24px rgba(0,0,0,0.4);
  transform:rotate(3deg);
  width:160px;flex-shrink:0;
}
.polaroid img{width:100%;height:160px;object-fit:cover;object-position:top center;display:block;}

/* ── FOOTER ── */
.port-footer{border-top:1px solid var(--color-divider);padding:var(--space-12) var(--space-5);}
.footer-inner{max-width:var(--max-w);margin-inline:auto;display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:var(--space-4);}
.footer-name{font-family:var(--font-serif);font-size:0.9375rem;font-weight:700;color:var(--color-muted);}
.footer-links{display:flex;gap:var(--space-6);list-style:none;}
.footer-links a{font-size:0.875rem;color:var(--color-muted);transition:color var(--transition);}
.footer-links a:hover{color:var(--color-text);}

/* ── REVEAL ── */
.reveal{opacity:0;transform:translateY(16px);transition:opacity 0.55s ease,transform 0.55s ease;}
.reveal.visible{opacity:1;transform:translateY(0);}

/* ── RESPONSIVE ── */
@media(max-width:640px){
  .page-wrap{padding-inline:var(--space-4);}
  .nav-inner{padding-inline:var(--space-4);}
  .ai-metrics{grid-template-columns:1fr;}
  .ai-section{padding:var(--space-8);}
  .nav-links,.nav-cta{display:none;}
  .nav-hamburger{display:flex;}
  .recognition-item{flex-direction:column;}
  .cta-row{flex-direction:column;}
  .project-item{grid-template-columns:1fr;}
  .project-arrow{display:none;}
}
@media(prefers-reduced-motion:reduce){
  .reveal{opacity:1;transform:none;transition:none;}
}
`}</style>

<nav className="nav">
  <div className="nav-inner">
    <a href="#" className="nav-logo">A.M.</a>
    <ul className="nav-links">
      <li><a href="#work">Work</a></li>
      <li><a href="#ai-story">AI Story</a></li>
      <li><a href="#projects">Projects</a></li>
      <li><a href="#consulting">Consulting</a></li>
    </ul>
    <a href="https://www.linkedin.com/in/argodeep" target="_blank" rel="noopener noreferrer" className="nav-cta">Connect</a>
    <button className="nav-hamburger" aria-label="Open menu" id="hamburger"><span></span><span></span><span></span></button>
  </div>
</nav>

<div className="nav-mobile-menu" id="mobileMenu">
  <a href="#work">Work</a>
  <a href="#ai-story">AI Story</a>
  <a href="#projects">Projects</a>
  <a href="#consulting">Consulting</a>
  <a href="https://www.linkedin.com/in/argodeep" target="_blank" rel="noopener noreferrer">Connect ↗</a>
</div>

<div className="page-wrap">

  <section className="port-hero" id="hero">
    <div className="photo-wrap reveal">
      <div className="polaroid">
        <img src="/dp.jpeg" alt="Arghyadeep Majumder" loading="eager" />
      </div>
    </div>
    <h1 className="hero-name reveal">Arghyadeep<br />Majumder</h1>
    <p className="hero-specialty reveal">Senior Engineer &<br />Solution Architect</p>
    <p className="hero-summary reveal">
      <strong>7+ years</strong> designing and shipping products end to end — from microservices to React & React Native frontends — for <strong>600K+ users</strong> across <strong>1000+ global clients</strong>. Every layer, every cycle.
    </p>
    <div className="hero-links reveal">
      <a href="#work" className="hero-cta">See my work →</a>
      <a href="https://github.com/argodeep" target="_blank" rel="noopener noreferrer" className="hero-link-plain">GitHub ↗</a>
      <a href="https://twitter.com/argodeep" target="_blank" rel="noopener noreferrer" className="hero-link-plain">Twitter ↗</a>
    </div>
  </section>

  <hr className="section-divider" />

  <section className="port-section" id="work">
    <h2 className="section-heading reveal">Work Experience</h2>
    <div className="how-body reveal">
      <p>I own problems end to end: clarifying ambiguous product requirements, designing the system, and shepherding features from design docs into production. Over the last <strong>7+ years</strong>, I've been building and evolving engagement products at scale — balancing reliability, UX, and delivery speed for enterprise customers.</p>
      <p>Day to day, that means defining service boundaries, shaping APIs that work well for both web and mobile, and tightening feedback loops with observability, CI/CD, and close collaboration with design and customer-facing teams. I care less about buzzwords and more about whether what we ship actually moves metrics.</p>
      <p>Currently building <strong>Empuls</strong>, Xoxoday's employee engagement platform — across surveys, rewards, wellness, and mobile — using <strong>AI tools as force multipliers</strong> to ship faster without compromising quality.</p>
    </div>
    <div className="tags reveal">
      <span className="tag">React</span>
      <span className="tag">React Native</span>
      <span className="tag">Node.js</span>
      <span className="tag">TypeScript</span>
      <span className="tag">Microservices</span>
      <span className="tag">MongoDB</span>
      <span className="tag">WebRTC</span>
      <span className="tag">WebSockets</span>
      <span className="tag">Expo</span>
      <span className="tag">CI/CD</span>
      <span className="tag">AWS</span>
      <span className="tag">System Design</span>
    </div>
  </section>

  <hr className="section-divider" />

  <section className="port-section">
    <h2 className="section-heading reveal">Impact Stories</h2>
    <div className="impact-list">
      <div className="impact-item reveal">
        <span className="impact-num">01</span>
        <p className="impact-text">Led a <strong>3-month migration</strong> of a production mobile app from Flutter to React Native (Expo + Hermes) with a 2-developer team — migrating 1000+ files and 500+ components, using AI tooling to replace months of manual engineering work.</p>
      </div>
      <div className="impact-item reveal">
        <span className="impact-num">02</span>
        <p className="impact-text">Owned a <strong>large-scale survey stack for 4+ years</strong> — from microservices and MongoDB pipelines to UI, reliably handling 100K+ survey users and high-volume response processing as adoption grew.</p>
      </div>
      <div className="impact-item reveal">
        <span className="impact-num">03</span>
        <p className="impact-text">Helped shape engagement experiences across surveys, rewards, social, wellness, and mobile — working closely with product, design, and customer teams to ship the <strong>right abstractions</strong>, not just close tickets.</p>
      </div>
      <div className="impact-item reveal">
        <span className="impact-num">04</span>
        <p className="impact-text">Built a <strong>collaborative online interview platform in 3 weeks</strong> during COVID — with shared cursors, WebRTC audio/video, live chat, and automatic test-case validation. Used by companies for real technical hiring.</p>
      </div>
    </div>
  </section>

  <hr className="section-divider" />

  <section className="port-section" id="ai-story">
    <div className="ai-section reveal">
      <p className="ai-label">AI Adoption Story</p>
      <p className="ai-quote">Ranked #1 on my org's AI usage leaderboard out of 91 engineers.</p>
      <p className="ai-body">When AI coding tools rolled out across the org, most engineers tried them. I rewired how I work around them — making AI a daily collaborator across architecture, code review, debugging, and migrations. Not a shortcut. A <em>thinking partner</em>. Prompt crafting, context management, and knowing <em>when not to trust the model</em> started compounding — I was shipping more, faster, without cutting corners on quality. Accepted diffs climbed because the suggestions were actually correct, and I knew the codebase well enough to validate them. By Q1 2026, the numbers were in: across 91 engineers, I had generated nearly <strong>2× the agent lines of code</strong> of the second-ranked engineer.</p>
      <div className="ai-metrics">
        <div className="ai-metric"><div className="ai-metric-val">12,550</div><div className="ai-metric-label">Accepted Diffs</div></div>
        <div className="ai-metric"><div className="ai-metric-val">5,984</div><div className="ai-metric-label">Tab Completions</div></div>
        <div className="ai-metric"><div className="ai-metric-val">641K</div><div className="ai-metric-label">Agent Lines of Code</div></div>
      </div>
      <p className="ai-footnote">Tool: Cursor AI (composer-1) · Org: Xoxoday · 91 engineers · Aug 2025 – Apr 2026. Accepted diffs matter most — code good enough to actually ship.</p>
    </div>
  </section>

  <hr className="section-divider" />

  <section className="port-section" id="projects">
    <h2 className="section-heading reveal">Projects</h2>
    <div className="projects-list">
      <a href="https://news.arghyadeep.com" target="_blank" rel="noopener noreferrer" className="project-item reveal">
        <div>
          <div className="project-title">MicroNEWZY</div>
          <p className="project-desc">Short news service experiment exploring micro-content consumption, navigation, and performance trade-offs on the web.</p>
        </div>
        <span className="project-arrow">↗</span>
      </a>
      <div className="project-item reveal">
        <div>
          <div className="project-title">YouTube Tutorial Maker</div>
          <p className="project-desc">Web app to record screen and camera together with offline support and PWA experience — making it easy to produce developer tutorials without heavyweight tooling.</p>
        </div>
      </div>
      <a href="https://youtu.be/fqwuPihkIi4" target="_blank" rel="noopener noreferrer" className="project-item reveal">
        <div>
          <div className="project-title">Google Lens-like Text Recognition</div>
          <p className="project-desc">React Native demo performing text recognition similar to Google Lens — experimenting with on-device scanning and UX around camera flows.</p>
        </div>
        <span className="project-arrow">↗</span>
      </a>
      <a href="https://www.linkedin.com/feed/update/urn:li:activity:6720534691264516096" target="_blank" rel="noopener noreferrer" className="project-item reveal">
        <div>
          <div className="project-title">Twitter UI in ReactJS</div>
          <p className="project-desc">Rebuilt core Twitter UI patterns in ReactJS to deepen understanding of component composition, layout, and interactive feed performance.</p>
        </div>
        <span className="project-arrow">↗</span>
      </a>
      <div className="project-item reveal">
        <div>
          <div className="project-title">Other Experiments</div>
          <p className="project-desc">Stock Android-style calculator for the web · QR Gate Pass · Barcode Warranty Verifier · Complaco (multi-platform document storage) · Several WebRTC/WebSocket-based apps exploring real-time UX and reliability challenges.</p>
        </div>
      </div>
    </div>
  </section>

  <hr className="section-divider" />

  <section className="port-section">
    <h2 className="section-heading reveal">Recognition</h2>
    <div className="recognition-list">
      <div className="recognition-item reveal">
        <div>
          <div className="recognition-title">Impact in the Shadows · Circle of Excellence · Rockstar Awards</div>
          <div className="recognition-org">Leading critical deliveries, shipping complex solutions under tight timelines, and driving high-impact features into production.</div>
        </div>
        <span className="recognition-badge">Xoxoday</span>
      </div>
      <div className="recognition-item reveal">
        <div>
          <div className="recognition-title">HackerRank Certifications</div>
          <div className="recognition-org">JavaScript & React</div>
        </div>
        <span className="recognition-badge">HackerRank</span>
      </div>
      <div className="recognition-item reveal">
        <div>
          <div className="recognition-title">Employee of the Year & Cheers to Peers</div>
          <div className="recognition-org">Jointly selected Employee of the Year (2019–2020) · 50+ peer recognitions for collaboration and delivery.</div>
        </div>
        <span className="recognition-badge">SKaaS</span>
      </div>
    </div>
  </section>

  <hr className="section-divider" />

  <section className="port-section" id="consulting">
    <h2 className="section-heading reveal">Consulting</h2>
    <div className="consulting-body">
      <p className="reveal">Outside my role, I help teams and engineers with architecture reviews, career guidance, and practical advice on growing as a hands-on engineer.</p>
      <p className="reveal">Available for consulting on <strong>product architecture</strong> and <strong>Senior / Staff IC career paths</strong>.</p>
      <div className="cta-row reveal">
        <a href="https://www.linkedin.com/in/argodeep" target="_blank" rel="noopener noreferrer" className="btn-primary">Get in touch →</a>
        <a href="https://twitter.com/argodeep" target="_blank" rel="noopener noreferrer" className="btn-ghost">Twitter ↗</a>
      </div>
    </div>
  </section>

</div>

<footer className="port-footer">
  <div className="footer-inner">
    <span className="footer-name">Arghyadeep Majumder</span>
    <ul className="footer-links">
      <li><a href="https://twitter.com/argodeep" target="_blank" rel="noopener noreferrer">Twitter</a></li>
      <li><a href="https://github.com/argodeep" target="_blank" rel="noopener noreferrer">GitHub</a></li>
      <li><a href="https://www.linkedin.com/in/argodeep" target="_blank" rel="noopener noreferrer">LinkedIn</a></li>
    </ul>
  </div>
</footer>

<script dangerouslySetInnerHTML={{ __html: `
const hamburger = document.getElementById('hamburger');
const mobileMenu = document.getElementById('mobileMenu');
if (hamburger && mobileMenu) {
  hamburger.addEventListener('click', () => {
    mobileMenu.classList.toggle('open');
    hamburger.setAttribute('aria-expanded', mobileMenu.classList.contains('open'));
  });
}
document.querySelectorAll('.nav-mobile-menu a').forEach(a => {
  a.addEventListener('click', () => mobileMenu && mobileMenu.classList.remove('open'));
});
const reveals = document.querySelectorAll('.reveal');
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) { e.target.classList.add('visible'); observer.unobserve(e.target); }
  });
}, { threshold: 0.06, rootMargin: '0px 0px -32px 0px' });
reveals.forEach((el, i) => {
  el.style.transitionDelay = (i % 4) * 55 + 'ms';
  observer.observe(el);
});
` }} />
